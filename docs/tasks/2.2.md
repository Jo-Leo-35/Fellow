# Subtask 2.2 — Backend core APIs

你是唯一實作 Agent/CLI session，`gpt-5.6-sol`、`xhigh`，工作目錄 `/home/bojack/workspace/FutureAI`。必須等 1.2 與 2.1 handoff 完成。

## 規則與範圍

- 先讀 full-stack SDD、執行計畫、固定 API 契約、`docs/handoffs/{1.1,1.2,2.1}.md`，再讀 backend 與前端 typed client/interface。
- 只改 `backend/`（core routers/services/必要基礎）與 `docs/handoffs/2.2.md`。不改前端、SDD、契約、根目錄設定，不 spawn、再開 CLI、commit/reset/還原或讀取輸出秘密。

## 實作與驗收

1. 實作契約全部 core endpoints：profile GET/PUT、明確 consent memory POST/DELETE、history list/detail/delete、resources list/detail、learning materials、alerts list/read、JPEG/PNG upload/authenticated content、teacher/government snapshot。
2. 建立可直接驗證的最小 server-configured access-code exchange、opaque session 與 auth dependency，後續 4.1 加固。Identity/role/scope 由伺服器決定，不預設任何 request 為 Demo student；沒有設定 secret 就拒絕交換。測試用臨時設定，不能把固定可用 code 放 source。
3. Profile replace/validation 配合 client；memory 只能接受本人、未過期、server-issued suggestion 且 consent:true，相同接受可重試。拒絕不寫入；刪 memory 清除對應 profile feature，避免推薦仍使用已刪記憶。History 直接回 snapshot，不呼叫 Agent/重建來源；刪對話保留已同意 memory 與穩定統計。
4. 推薦/通知 matching 依 persisted profile，正確處理 region 與已讀；有限清單空結果為 []。政策 eligibility 採保守規則，embedding 不能決定正式資格。
5. Upload 限 5 MiB、JPEG/PNG MIME 與 magic、正確影像解碼、隨機檔案 id/安全顯示檔名；內容只有 owner 可讀，不暴露 server path。413/415/422 與其他 API error 都用統一 envelope。
6. Teacher 先限制 session scope 再套 period/class/subject/threshold，提供既有所有分頁所需 snapshot。Known practice/animation 分母與 null 正確；新聊天只增加實際 question/gap。Government 只查 Insight aggregate projection，不 join profile/users/raw conversations；taxonomy/未知 region 保守處理，counts 與 previous windows 一致。
7. 不做 Agent pipeline。提供明確的 session、DB、quota schema、persist/insight 接點給 3.1/4.1；需先實作的 usage 基礎不得回虛构常數假装持久用量。

2.1固定Demo dataset anchor僅供示範學習/Insight/dashboard資料；session與suggestion到期、daily quota/reset_at、idempotency TTL必須使用真實UTC，rate/timeout用真實時間或monotonic clock。不得用固定seed日期凍結安全與用量計時，請於本Subtask handoff交接。

用臨時 DB 的 API smoke/integration 驗證 CRUD、consent 重試、snapshot replay、role/owner 禁止、upload/validation 與 dashboard privacy/計數；不呼叫供應商。最後寫自己的 handoff，列修改/API、驗證與 3.1 所需接點及仍待 4.1 的安全工作。
