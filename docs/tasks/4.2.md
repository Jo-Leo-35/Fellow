# Subtask 4.2 — Docker / configuration / runbook

使用者已允許目前對話直接完成，不再開CLI；等4.1完成，工作目錄 `/home/bojack/workspace/FutureAI`。保留單一Task4 owner與既有責任範圍，直接使用工具實作與驗收。

使用者最新要求後端優先，本階段3.2尚未開始。先完成backend的Docker啟動、auth/core API與offline Agent HTTP smoke、持久化，以及frontend靜態建置/反向代理；完整UI操作流程待3.2接線後由5.1實測。文件須如實區分這兩者，不宣稱已完成尚未接線的瀏覽器Demo。真實provider設定留待後補，無key不阻塞offline後端驗收。禁止修改 `~/.codex` 或全域MCP/Codex設定。

先讀full-stack SDD、計畫/契約與所有handoff，檢查package/vite/backend啟動、seed/index/config與現有README。只改Dockerfiles/Compose/config、啟動scripts、`.gitignore`、`.env.example`、`README.md`、`docs/demo-runbook.md`、自己的handoff。必要改backend啟動檔需先向協調者報明具體依賴；不可悄改Agent/業務邏輯。不得spawn/另開CLI/commit/reset/還原或讀取輸出使用者secret。

1. 單機 `docker compose up --build -d` 啟動可Demo的前後端。Frontend建置後供static+SPA/legacy routes，反向代理same-origin `/api/v1`及適當health/docs；backend FastAPI，SQLite/Chroma/uploads持久volume。不得新增SDD禁止服務。
2. Seed/index startup可重跑且不覆寫使用者狀態；healthcheck/readiness依賴正確，live索引失敗不可冒充ready。配置重啟、非root執行與可寫volume，排除node_modules、env、SQLite、uploads、.codex-runs等build context。
3. `.env.example`只用空secret/placeholder。提供本機產生secret與設定三角色access code的明確命令/方式，不在README、CLI log或bundle列出真secret。保留既有.env不覆寫；測試用獨立臨時env/project/volume與不衝突port。
4. 明確區分offline_demo與live generation/embedding配置及限制、模型/base URL/timeout、CORS、quota、persistent data位置、啟停/seed/index/健康檢查/測試命令。Runbook有可操作三角色Demo步驟與不需key的offline情境；不要宣称真實政策資格。
5. `.gitignore`涵蓋CLI artifacts/venv/DB/Chroma/uploads與secrets，不能把應提交seed誤排除。不commit。

既有 `backend/data/{curriculum,policies,alerts}` 是需打包的 seed 原始檔；SQLite/uploads/Chroma 是 runtime data。Volume 路徑應透過 `APP_DATA_DIR` 等設定與 source catalogs 分開，避免掛載空 volume 隱藏教材／政策檔，或日後映像更新仍讀到旧 catalog 副本。

實際compose config/build/up，以獨立project跑health、前端、Swagger、auth/Agent smoke、重啟資料持久。若Docker權限/daemon不能使用，記精確錯誤，別宣稱已成功。不要停止既有5173或刪使用者volume；測試資源可清除但保留驗證紀錄。Handoff附啟動命令、設定、驗證與5.1測試接點。
