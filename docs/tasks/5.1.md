# Subtask 5.1 — API / browser integration verification

唯一實作 CLI，`gpt-5.6-sol`、`xhigh`，等4.2，工作目錄 `/home/bojack/workspace/FutureAI`。

先讀full-stack SDD、API契約、計畫/runbook、所有handoff，再讀現有integration/audit scripts與API/UI。只改 `backend/tests/`、`tests/`、測試用途`scripts/`、必要`package*.json`測試命令、`docs/integration-report.md`與自己的handoff；不改產品實作，缺陷交5.2。不得spawn/另開CLI/commit/reset/還原，不讀取輸出secret。

1. 建立可重跑隔離DB/Chroma/設定的API integration，依契約驗證真實服務行為：session/角色/owner、profile CRUD、memory consent/拒絕/刪除/重試、history/source snapshot、resources/materials、alert/read、upload/download。
2. Agent驗證learning/resource/auto/追問、offline未知503、nullable rendering契約、source id、同key/alias/conflict、最多一Insight。Mock provider驗證live request/schema/embedding/error/timeout且不降級；無key不宣稱實際live通過。
3. 驗證並發quota/relogin/重啟/失敗返還，StrictMode/home導航首題單次HTTP請求，歷史reopen只GET、用量不變。Teacher新chat改question/gap但不改practice/animation分母；government正確region/topic delta及所有巢狀payload/CSV不含個人資料。
4. 真實Playwright browser三角色完整Demo flow，至少320px手機與桌機、六學習/資源入口及teacher/government分頁、filter/detail/CSV/偏好。既有audit因登入/API變化可調整測試setup與fixtures，但不得刪關鍵斷言或改產品來假過。
5. Docker隔離project實際啟動/重啟驗證SQLite、memory、read、history、quota與Chroma持久。保持使用者既有Vite/資料完整；測試secrets不寫log/report，失敗screenshot不得含access code/token。
6. `npm run typecheck`、build、backend integration、browser tests與必要Docker smoke。不要無理由重跑已過檢查。

既有 `scripts/learning-audit.mjs` 的 `checkProductCopy` 禁止所有「示範」字樣，與新契約明確顯示「離線示範」衝突。只對合法模式標示調整斷言，仍檢查其餘產品文案與互動；不要為通過舊測試隱藏模式或刪除整個檢查。

報告每项通過/失敗/未測、命令、環境、可重現步驟與最小缺陷定位，不把跳過當通過。完成自己的handoff及5.2具體修正清單；若無缺陷明說，5.2只需最終驗收，不發明重構。
