# Subtask 5.2 — Integration fixes / final verification

使用者已允許目前對話直接完成，不再開CLI；等5.1完成且先前owner皆退出，工作目錄 `/home/bojack/workspace/FutureAI`，保留單一Task5 owner與既有責任範圍，直接使用工具修正與驗收。

先讀full-stack SDD、計畫/契約、所有handoff、runbook與integration report，閱讀每個待修缺陷相關程式後再動手。只修改5.1實際發現缺陷所需檔案、驗收文件/自己的handoff；不得重定義SDD或擴需求。不spawn/另開CLI/commit/reset/還原，不讀取輸出secret。

1. 逐一重現並修復5.1缺陷，修正以契約與既有UI行為為準；必要回歸覆蓋缺陷本身，不能降低斷言/忽略error來假過。若驗收發現新重大同範圍缺陷，一併修復並記錄。
2. 執行受影響tests，再完成typecheck/build、backend integration、完整browser Demo及Docker smoke/重啟持久驗證。保留使用者Vite、volume與未提交內容，不自行發布/commit。
3. 最終report/runbook反映實際可用啟動方式、學生/教師/政府流程、offline支援範圍、live設定與測試狀態、明確剩餘限制。Live缺key是未測，不可記成成功；外部限制記可重現證據。
4. 確認沒有client demo generator取代API的假success、沒有secrets進bundle/repo/report、沒有government私人資料、沒有reload/retry重扣/重複Insight。只依實際證據宣稱完成。

最終文件核對包含前序handoff的過期狀態文字：例如3.1實作CLI留下「待獨立驗收」，但execution plan已記錄協調者19/19與所有原失敗案例通過。可依execution plan中的具體驗收證據補註/更新這類handoff狀態，不重寫其歷史結果，也不把未測live provider改成通過。

完成 `docs/handoffs/5.2.md`：修復清單/檔案/API變動、驗證結果、仍未解問題、最短啟動與Demo方式。回報主協調者更新執行計畫；不自己改其他任務狀態。
