# Subtask 3.2 — Frontend integration

使用者已允許目前對話直接完成，不再開CLI；工作目錄 `/home/bojack/workspace/FutureAI`，保留單一Task3 owner與既有責任範圍。依使用者最新後端優先要求，等3.1與4.1/4.2完成再直接使用工具實作。真實provider設定後補，以已驗收offline API完成本階段接線。禁止修改 `~/.codex` 或全域MCP/Codex設定；缺文件工具直接官方web。

使用者再次明確接受 Agent 先 Fake Demo。AI回覆使用既有offline_demo API與清楚模式標示即可；不配置真實模型、不擴充live品質。其餘profile/memory/history/resources/alerts/dashboard/auth/usage仍要真實API與持久狀態，不能只在前端假裝成功。

先讀 full-stack SDD、計畫、API 契約、所有已完成 handoff，再讀所有 pages/shells/data、typed client 与 backend OpenAPI/實作。只改 `src/`（必要 client/types 可增量）及 `docs/handoffs/3.2.md`。不 spawn、另開 CLI、改 SDD/契約/backend/root config、commit/reset/還原或讀取輸出秘密。

## 接線

1. 完整覆蓋契約第2節所有 routes/aliases/actions。用既有 TanStack Query/fetch layer 管理 server state，保留畫面、互動、六理化動畫/來源 modal、六資源分類、教師五分頁/政府六分頁與 responsive layout。
2. 最小 runtime access gate 输入 server access code，角色/名稱/模式取 session，token 僅 sessionStorage/記憶體；access code 不保存。401 清 session與個人 query cache，角色切換不得殘留前一角色資料。依已完成4.1交接落實安全接點，5.1會做完整瀏覽器驗收。
3. 首頁 auto chat、學習/資源首題及追問走 `/agent/chat`。submission id/Idempotency-Key 在使用者動作建立，StrictMode 共用 in-flight promise。navigation 傳既有 submission/result，不再用 q 重發。直接 q URL 亦穩定去重；history reopen 只 GET detail，不發 Agent/重扣 quota。
4. 圖片限制JPEG/PNG，先實際 upload再傳id，不能只傳檔名；歷史圖片透過帶認證fetch/blob取回。無來源/未知animation topic/無practice等 nullable payload 正常呈現。Offline清楚標示；unsupported/provider/network/cancel/429 有正確狀態，不造假回答。
5. Profile GET/PUT、memory 同意/拒絕/刪除、兩份history drawer、resources list/detail、alerts matching/read與bell未讀點全部共用 API/cache。移除只改React state的假成功；保留有明示的session-only alert dismiss。
6. Teacher/government 使用 server snapshots，停止呼叫本機資料 generator 當最新資料。圖表/CSV/detail/filter 依 server資料，分母使用契約且 null 顯示未知；teacher不從gap猜作答，government不補個人明細。本機 preferences/review plans/tracking照舊並清楚界定。
7. loading/error/empty/retry不破壞既有流程；retry沿用同一次submission key。刷新後持久狀態正確、query invalidation更新用量/歷史/通知，不顯示錯誤為成功。不要把原始錯誤堆疊/secret放UI。

## 驗證

跑 `npm run typecheck`、`npm run build`；實際啟動backend並以瀏覽器驗證學習→追問→歷史、資源→同意記憶、profile/alerts、三角色dashboard。不停止使用者5173 Vite；若需隔離用別port。Chromium libraries可用 `.codex-runs/browser-libs/root/usr/lib/aarch64-linux-gnu`。不修改測試來掩飾問題，原audit假資料依賴留5.1處理並記錄。Handoff列API接線、驗證、4.1安全接點的落實結果與未解問題。

## 前置只讀盤點（行號為接線前位置）

- `StudentHomePage.tsx:129/139/144` 的首頁 Enter/submit 統一建立一次 submission；上傳成功後送 auto，導航攜帶原提交與結果。
- `LearningChatPage.tsx:303`、`ResourceChatPage.tsx:627` 依 URL key 重掛元件；直接 q 入口需每次導航/session 穩定去重，成功後保存真正 conversation ID，刷新/返回只取 detail。
- `StudentHomePage.tsx:290`、`ResourcesPage.tsx:183` 有兩份 history；目前把 title 放入 history/q，必須改成 conversation ID。不要把歷史標題重新送模型。
- 學習追問在 `LearningChatPage.tsx:209/220/294`；資源追問在 `ResourceChatPage.tsx:695/725/733/947`。新動作新 key，同次失敗重試保留原 key/body；component cleanup 不可取消其他 consumer。
- 首題導向還包括首页主題卡、learning教材庫、ResourcesPage兩處、ResourceChatPage、AlertsPage、TeacherDashboardPage。教師只有 curriculum `GET /learning/materials` 權限，教材預覽可沿用來源/教材 modal；不可因教師點教材就自動送 student-only Agent 或暗中換角色。
- `main.tsx` QueryClient 與 `DashboardShell.tsx:133` 的角色連結須接 session cleanup/access gate。1.2 submission registry 僅記憶體；頁面自行保留穩定 submission，401/換token要清 query、個人state與附件blob URL，並重掛個人頁。
