# 5.2 — Integration fixes and final verification

狀態：5.1 → 5.2 全部完成。四項必要產品修復、全部browser與Docker持久性驗收均通過，測試fixture已停止並保留資料。真實Agent/provider後補，沒有未授權的live設定或外部呼叫。

## 修復與範圍

| 問題 | 最小修正 | 回歸 |
| --- | --- | --- |
| 六學習情境原作類比/理由縮短 | 六catalog恢復11個文字值；seed僅升級精確已知舊預設，practice核對題意，自訂與history snapshots保留 | 新2個migration tests + 六browser完整練習/類比 |
| API真文件數到達後清單仍保留舊4項狀態，無法完成 | `ResourceChatPage.tsx` 的ChecklistModal以推薦message ID與文件集作state identity | 六分類全部勾選完成、重開保留、切換重設 |
| 首頁auto Newton第二定律被『第二定律』generic hint誤分到entropy | `agent.py`增加五個精確物理定律名稱hint；不改generic route或放寬coverage | 新3個真API tests、原首頁無topic圖片輸入成功且只POST一次 |
| Learning/Resource首卡省略message.text中的圖片限制告知 | 兩page首卡顯示完整message.text，其他結構欄位與sources保留 | 兩類圖片首次回答與history GET重載都有明確告知 |

詳細catalog與routing交接見 [5.2-catalog.md](5.2-catalog.md)、[5.2-routing.md](5.2-routing.md)。API契約、產品配額20及10/30 rate均未更動。

其他改動只在audits/helpers/fixture、測試指令與文件：全部真auth/API、合法離線標示、稀疏CSV群組、profile URL重載、真history mode與targeted alerts setup。保留原動畫/分類/dashboard/CSV關鍵斷言，另覆蓋六學習與六資源各18個追問。Client偶發失敗是測試server跨案handler干擾，Root修測試隔離後連續10輪通過；沒有產品自動retry bug。

## 驗證結果

- Backend完整 **37/37 passed**（64.774秒），包含auth/role/owner、upload、quota/並發/退款、去重alias、來源/Insight、nullable、seed與持久性、新catalog/routing regressions。`.codex-runs/5.2-root-backend-tests.log`。
- Browser六組 **87/87 passed**：learning26、resources27、dashboard13、interaction7、integration7、visual7。含320px/390px手機與1024/1440桌機、21組政府聚合、CSV/追蹤/偏好、三角色真gate/login/reload、teacher兩aliases/18chunks/0Agent、首頁單次POST、GEThistory用量不變、兩類圖片告知。
- API client真HTTP **9/9**，另連續10輪 **90case全部通過**；`.codex-runs/5.1-client-isolation-repeat.log`。
- Typecheck、Docker production build/tsc/Vite、Ruff與`git diff --check`通過。
- 真正serve的HTML與兩個bundled assets比對三角色fixture codes及session token，未含憑證。登入token只存sessionStorage，不在URL/localStorage，登出清除。
- 七頁最終截圖/HTTP200/0水平overflow/0browser error；`.codex-runs/5.2-visual.log`。人工抽看農業390與熵320畫面，卡片/公式/固定輸入區正常。

- Docker restart與down/up重建兩階段均通過：全部SQLite rows、uploads bytes、Chroma IDs/documents/metadata/embeddings、sessions/quota/memory/read/history/三角色API快照保持；alias replay零額外寫入，relogin不重置用量。`.codex-runs/5.2-restart.log`。
- 專屬fixture `futureai-integration-b63c9d8e757a` 已`down`，container/network移除，volumes/env/state保留，`status`沒有运行服務；`.codex-runs/5.2-fixture-down.log`。要重跑用`python3 scripts/integration-fixture.py up`。

完整命令、環境、修前證據、結果與重跑方式見 [integration-report.md](../integration-report.md)。

## 正常展示環境

主協調者已用最終source重建 `futureai-t42-681542` 至 <http://127.0.0.1:45465>，兩服務healthy、offline_demo、DemoBearer Swagger正常。重建前後22個非catalog資料表逐表hash與2張圖片hash相同，Chroma18/12，正常學生每日配額20保持。正常環境三角色UI登入/reload全通、0Agent POST、0pageerror；`.codex-runs/5.2-normal-deploy-report.json`、`5.2-normal-browser.log`。

本輪臨時Vite45466已由Root關閉；原使用者Vite5173、pathwise容器與既有dirty檔案保留。沒有commit、reset或全域設定修改。

## 最短啟動與操作

```bash
python3 scripts/create-demo-env.py
docker compose up --build -d
```

新部署預設 <http://localhost:8080>。在本機編輯器查看私有`.env`內對應存取碼，學生首頁、教師`/teacher.html`、政府`/government.html`各以自己的角色登入。學生先問「請解釋牛頓第二定律」，再看動畫／來源／追問；資源可問「家裡菜園颱風受損，有補助嗎？」；教師看授權班級與教材，政府看匿名聚合與CSV。完整步驟、持久化/備份/重啟見 [demo-runbook.md](../demo-runbook.md)。

唯一後補項目是外部真實Agent/provider設定與實測。Fake支援六理化主題及六類資源的既有內容；無關問題/image-only會清楚拒絕且不扣次，資格/未知URL與期限不編造。未把localhost mock與Fake成功宣稱為live通過。
