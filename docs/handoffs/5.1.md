# 5.1 — Integration verification handoff

2026-09-05：真實前後端整合第一輪已完成缺陷定位，交同一 Task5 owner 接續 5.2。完整最終結果以 `docs/integration-report.md` 與 `5.2.md` 為準；此文件保留修復前證據。

## 環境與覆蓋

- 專屬 Compose project `futureai-integration-b63c9d8e757a`、localhost 動態 port 59653、全新 SQLite / Chroma volumes。三角色經真 session exchange；另以 UI 登入驗證 gate、reload、角色切換。
- Fixture private env 在 repo 外、mode0600，access codes/token 不输出。只有此 fixture quota 與 rates 提高至1000；產品 quota20、exchange10/min、Agent30/min未改。低限額/429/退款由既有隔離後端測試驗證。
- 修復前 backend fullsuite32/32，log `.codex-runs/5.1-root-backend-tests.log`；新增兩組持久性/provenance回歸已納入。
- Learning26案首輪20通過/6失敗；resource27案在合法標題修正後20通過/7失敗；dashboard13案8通過/5失敗；interaction7案3通過/4失敗；integration6案4通過/2失敗。真實產品缺陷與測試假設如下分開記錄；最終必須保留全部覆蓋重驗。

## 交 5.2 的真實缺陷

1. 六主題原作類比/解題理由被 backend catalog 縮短，既有 substantive reasoning 斷言揭露回歸。限定恢復 analogy/practice.explanation，精確舊預設局部升級，保留自訂 catalog 與歷史 snapshots。由原 Task3 owner 交付 `5.2-catalog.md`。
2. Resource ChecklistModal 在 API 返回前用舊4項文件初始化勾選陣列；真 API 2/3項文件到達後，按鈕仍顯示剩4項、全部可見勾完仍無法完成。狀態必須跟隨實際推薦/文件集，重開同份清單保留勾選，換分類/對話重設。
3. 首頁 mode=auto +『請解釋牛頓第二定律』誤路由 entropy：generic『第二定律』4分壓過『牛頓』2分，既有 Newton 內容無法匹配而503。保留原 UI request 作回歸，修正具體主題消歧；不擴 Fake provider 能力。

4. Learning/Resource 首張結構卡只顯示 payload.summary，丟掉 API message.text 的圖片無法辨識告知。實際使用同一已上傳 PNG + 明確 topic/category 驗证兩mode皆 API disclosure=true/UI=false；首卡須呈現完整回答文字，歷史 GET同樣保留。

## 測試接線／隔離修正

- 全部 audits 共用真 auth 與 API；保留六學習動畫/練習/來源、六分類詳情/追問、全部 dashboard/CSV/偏好、手機/桌機斷言。
- 只豁免契約要求的離線模式、資源標題精確『（Demo 資源方向）』及 server scope_label；沒有移除整個文案檢查。
- 政府 CSV 依真 daily_aggregates 的 region/topic 稀疏群組比對列數，四個計數總和須等於同次 filtered API，而非固定6行；21組 API聚合及個資禁入檢查保留。
- 農業 query fixture 原先只有『菜園颱風』，改完整既有支援語句『家裡菜園颱風受損，有補助嗎？』；不修改分類或放寬 unsupported。
- Profile 改驗真正保存/只讀狀態、API與reload，而非過時本地toast。Resource 追問等待真HTTP成功且限定main，避免textarea與已送出user bubble重複匹配。
- Alerts 從真 profile 建立已知農業條件後取 server匹配的critical alert，驗已讀與reload持久性，避免依賴前一失敗案例留下的記憶資料。
- Teacher preview保留teacher身份、同tab、18chunks、零Agent POST；alias標題匹配『化學動態平衡』。
- 每案立即輸出已遮蔽錯誤，finally寫階段progress report，避免browser中断丟失原因。
- API client第9案曾不穩定是測試server跨案共用handler，被前案abort後遲到request干擾。Root改為按fake token固定route；9/9且連續10輪90case通過，log `.codex-runs/5.1-client-isolation-repeat.log`。沒有產品自動retry bug。

## 接續驗收

5.2 修必要產品檔、保留原volume重建，重跑全部browser、受影響backend/fullsuite、typecheck/build與Docker restart/down-up持久性。報告、README/runbook更新為實際完成的三角色操作。真實外部Agent未設定/未測，後補；不把localhost mock或離線成功當live驗收。
