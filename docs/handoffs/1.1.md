# Handoff 1.1 — Frontend / API Alignment

日期：2026-09-05

## 完成內容

- 完整盤點四份規格、`src/main.tsx`、全部 `src/pages/`、student/dashboard shells、profile/history UI、learning animation、指定四份 data，以及既有 API client/types。
- 1.1 是第一個 Subtask；開始時沒有前序 `docs/handoffs/` 文件。已確認 full-stack SDD 的 frontend/backend 正文分別與獨立 SDD 完全一致。
- 建立逐 route/page 的現況資料來源、操作、API/view model 與 local/API state 邊界。
- 定義 `/api/v1` wire schema、endpoint/status/query/ownership、snake_case → camelCase adapter、structured Agent response、source/history replay、dashboard snapshot、error/timeout/cancel 與可重現 Demo flow。
- 明確保留六類資源、rich 理化回答/來源/practice/follow-up/animation、教師五分頁與政府六分頁；教師只含授權教學資料，政府只含 Insight aggregate。
- 定義 server-configured Demo session、server role/scope、sessionStorage token 策略、usage、持久原子 quota reservation/finalize/refund、rate limit 與 Agent idempotency。主要入口是 `/agent/chat`；`/chat` 只可為同 service alias。
- 完成「原 session 驗收補充」六項最小增量：SDD wire 差異表、StrictMode/navigation single-flight、固定 Demo principal quota、教師已知結果 denominator、政府 category/region taxonomy，以及後續並行責任界線。

## 修改檔案

- `docs/api-alignment.md`（新增）
- `docs/handoffs/1.1.md`（新增）

未修改 source、SDD、package、README 或 execution plan；保留開始時已有的所有未提交修改。

## 新增／修改的 API 與 interface 契約

- Auth/usage：`POST /auth/demo/session`、`GET /auth/session`、`GET /usage`。
- Agent：`POST /agent/chat`；`POST /chat` 相容 alias；`AgentChatRequestWire`、完整 `AgentChatResponseWire`、learning/resource/memory/source/alert schemas。
- Core：profile GET/PUT、memory consent POST/DELETE、conversation list/detail/delete、resource list/detail、learning materials list、alert list/read、JPEG/PNG upload 與 authenticated content download。
- Dashboard：一個 `TeacherDashboardWire` snapshot 支援現有五分頁；一個 aggregate-only `GovernmentDashboardWire` 支援現有六分頁與 CSV/detail。
- Dashboard 補充：`TeacherCountsWire`／`TeacherRosterStudentWire` 新增 `animation_observation_count`（及 roster completion percentage），並固定 correctness/animation denominator；government 不增 endpoint，只補六 category 與六區 region normalization 規則。
- 共用：`UsageWire`、`AttachmentWire`、conversation replay messages、camelCase view contracts、`ApiErrorWire` 與完整 enums/nullability。

## 檢查與結果

- 規格完整性：以 byte comparison 確認 `hackathon-fullstack-sdd.md` 內 frontend/backend 正文等於兩份獨立 SDD；結果一致。
- Route 覆蓋：依 `src/main.tsx` 核對首頁、learning chat、resource chat、resources、alerts、teacher、government 與所有 `.html` aliases/profile query drawer；每個 page 都有最低接線方案。
- Endpoint/schema：endpoint 表共 21 條 route，逐項指向已定義或 inline 精確 response schema；Agent 與 core API 分開，wire 命名一致。
- Markdown 結構：code fence 計數為 16（成對），endpoint table、enum、optional/nullable、HTTP status、query 與 ownership 均已列出。
- Privacy：government schema 只有 date/region/topic/count/percentage/trend/aggregate insight，沒有個人、家庭、conversation/message/upload id；teacher schema只有授權名冊與學習摘要，並明文禁止家庭 profile/raw chat。
- 補充驗收：教師正確率／動畫率都有明確已知結果 denominator 且新 chat 不虛構完成紀錄；初始 `q` 與 navigation 共用 idempotency/single-flight，history reopen 僅 GET；未知 region 不進 aggregate；alias 相容範圍已明確限定為 route + canonical shape。
- Scope：最後的工作樹檢查顯示本 Subtask 只新增上述兩份允許文件；未執行測試（純文件 Subtask）。

## 未解問題

- Demo access code 的實際 secret、各角色 subject/scope、daily quota limit、rate-limit threshold 與對應 env 名稱留給 Task 4 server config；不得寫入 Vite 或 repo。
- 尚未提供 live provider key，故未驗證 OpenAI-compatible generation/embedding；契約要求無 key 時明確使用 `offline_demo`，不能宣稱 live 已通過。
- 現有 UI 沒有獨立 `/profile` route；契約採既有 `/?panel=profile` drawer，是否另加 route 不是 API 接線前置條件。

## 1.2 與 Task 2 需要知道的事項

- 主協調者已以程式驗證：strip 後的 `frontend-sdd.md` 與 `backend-sdd.md` 正文都完整包含在 `hackathon-fullstack-sdd.md`。後續工作者完整閱讀 full-stack SDD 即已涵蓋兩份 SDD 正文，無需重複全文；若規格檔日後改動則應重新驗證。
- 契約凍結後，Task 2.1 backend foundation 可與 1.2 shared client 平行進行：Task 2.1 只負責 server foundation/schema/routes，1.2 只負責前端 wire/view types、adapter/client，兩者以 `docs/api-alignment.md` 為共同邊界，不互改對方責任檔案。
- 1.2：先照 `docs/api-alignment.md` 建 wire/view types 與逐 endpoint typed client；特別處理 Bearer/sessionStorage、FormData boundary、204、error envelope、AbortController 與必填 `Idempotency-Key`。不要在 1.2 改 pages。
- 1.2：現有 `src/types/index.ts` 的 learning step 使用 `content`，但 rich learning page 使用 `body/sourceIds`；契約以後者為準，由 adapter 保留現有頁面能力。
- Task 2：seed/core data 要覆蓋六 learning topics/source chunks、六 resource categories、alerts/history/profile、教師授權 roster/summary/topic/status 與政府六區六主題 aggregates。Conversation detail 必須保存 structured payload 及 source snapshot。
- Task 2：所有 user id/conversation/attachment ownership 從 server session 驗證；政府 router 只能查 aggregate projection，教師不可取得家庭/原始對話。
- Task 2/4：quota ledger、message/Insight persistence 與 idempotency 要可持久、原子且 alias 共用；錯誤釋放 reservation，已 commit 的回答計次。

尚未做任何 backend 或 UI 實作，也沒有宣稱任何 API、browser flow、provider call 或測試已通過。
