# 5.2 — Restore authored curriculum feedback

## 修復

`5.1-learning.log` 的 6 個 flow 失敗源於 seed 將原作類比/解題理由縮短。此修復只恢復 `backend/data/curriculum/scenarios.json` 六個 scenario 的 `answer_payload.analogy` 與 `practice.explanation`，內容逐字對照 `src/data/learningScenarios.ts`。實際變更 11 個文字值：newton analogy 本來已相同，其餘 5 個 analogy 與 6 個 explanation 恢復原作。

未修改 frontend、其他測試、Task5 報告、Agent/provider 或題目結構。現有 practice question/options/answer_index 全部保留。

## 題意核對

- 六題正確答案 index 與原作一致：1 / 1 / 1 / 2 / 1 / 0。
- backend 的 newton、entropy、bonding、reaction-rate 選項是原作同義短版；恢復理由仍對應現有正確選項。
- equilibrium 題幹省略原作「在溫度不變時」，目前仍問對平衡系統加入催化劑的效果。恢復的原作理由明確寫出「同一溫度下」的適用條件，不會把變溫的平衡當成組成不變；未改題幹/選項。
- thermodynamics 題目/選項/index 與原作相同，理由恢復對外做功為正的符號約定。

## 既有資料局部升級

新增 `backend/scripts/catalog_migrations.py`，由 `seed_database` 在 catalog 已載入後執行：

1. 僅處理六個已知 demo scenario ID/topic。
2. analogy 必須逐字等於此次修復前的已知舊預設值才更新。
3. practice 除理由逐字等於舊預設外，question/options/answer_index 也必須逐一匹配舊預設，避免對自訂題目貼入不相符的理由。自訂理由、null practice、自訂題幹/選項/答案均保留；額外 practice metadata 也保留。
4. 使用完整舊 JSON 作條件更新，避免覆蓋讀取後發生的 catalog 修改；只改需要修復的兩個欄位，其他 payload/custom fields 與 scenario 欄位維持。
5. 已是新版或自訂值時不 UPDATE，重跑不改資料/updated_at。

升級不重建 DB、不重建歷史，不更新 `Message.structured_response` 或 `source_snapshot`。舊 history 保留當時的短版回答；之後新回答才使用恢復的 catalog。其他 mutable tables 仍依既有 seed 保留策略處理。

## 驗證

- 逐一讀取原作核對：6/6 analogy/explanation 完全一致；11 個文字值恢復；六題 question/options/index 均未修改。
- 新增唯一測試檔 `backend/tests/test_curriculum_catalog_migration.py`：以真正舊 catalog 建立 SQLite 與舊 history，確認六題升級；自訂類比/理由、改題幹/選項/index、null practice、額外 nested metadata、使用者/profile 修改均保留；重跑完全無變。
- migration tests 比較所有非 `curriculum_scenarios` table 的完整 rows，包含 messages 的 structured/source snapshots；另明確驗舊 newton history 理由仍為短版，與新版 catalog 不同。
- Targeted regression：
  ```sh
  PYTHONPATH=backend /tmp/futureai-backend-2-1-venv/bin/python -m unittest -v \
    backend.tests.test_curriculum_catalog_migration \
    backend.tests.test_persistence_integration.PersistenceIntegrationTest.test_seed_minimums_and_linked_insight_provenance
  ```
  3/3 通過（3.321 秒）。
- Ruff：
  ```sh
  /tmp/futureai-backend-2-1-venv/bin/ruff check \
    backend/scripts/seed.py backend/scripts/catalog_migrations.py \
    backend/tests/test_curriculum_catalog_migration.py
  ```
  通過。修改檔案 `git diff --check` 通過。

## 交回 Task5

本輪未修改任何執行中的 Demo DB/Compose，也沒有 fullsuite 或 Docker build。Task5 接續重建 backend 並執行原有 startup seed（自動升級已知舊 catalog），再跑 backend fullsuite 與完整瀏覽器回歸；不要刪除持久化 volume 或重寫 history 以取得新版文字。
